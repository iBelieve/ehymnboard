[project]
name = "device"
version = "0.1.0"
description = "eHymnBoard device firmware"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyserial>=3.5",
]

[dependency-groups]
dev = [
    "clang-format>=22.1.5",
    "clang-tidy>=22.1.7",
    "ruff>=0.16.0",
]
